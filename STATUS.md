Keeper `2026-09-20 03:55 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200; real post has Web3Forms markers). Apex still redirects to `www`, and both remain Blogger/GSE over HTTPS. DNS via public DoH/getent (box lacks `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. `cusdis.com` remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; latest `site` workflow is green. No newly available Squarespace/Google DNS login (Squarespace session/`crumb` only). No material change vs 02:58 CDT; Homey DNS action remains the blocker.
Keeper `2026-09-20 02:58 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200; real post has Web3Forms markers). Apex still redirects to `www`, and both remain Blogger/GSE over HTTPS. DNS via public DoH/getent (box lacks `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. `cusdis.com` remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; latest `site` workflow is green. No newly available Squarespace/Google DNS login (Squarespace session/analytics + Google AEC/NID/GAPS only). No material change vs 01:57 CDT; Homey DNS action remains the blocker.
Keeper `2026-09-20 01:57 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200; real post has Web3Forms markers). Apex still redirects to `www`, and both remain Blogger/GSE over HTTPS. DNS via public DoH/getent (box lacks `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. `cusdis.com` remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; latest `site` workflow is green. No newly available Squarespace/Google DNS login (Squarespace session/analytics + Google AEC/NID/GAPS only). No material change vs 00:58 CDT; Homey DNS action remains the blocker.
Keeper `2026-09-20 00:58 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a` brand colors, post covers, Drinks nav); `/drinks/` HTTP 200 and a real post route contains Web3Forms markers. Apex still 301-redirects to `www`; both continue serving Blogger/GSE over HTTPS. Public DNS still returns apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c1/c2/c3/c4.googledomains.com` (DoH/getent; box lacks `dig`/`nslookup`). `cusdis.com` remains HTTP **521**; comments remain Web3Forms. GitHub Pages API remains `status: built`, `cname: null`, `https_enforced: true`; latest `site` workflow is green. Light browser cookie check: Squarespace has `SS_SESSION_ID`/analytics/`crumb`/`ANONYMOUS_ID` only (no Domains-capable login); Google has `__Host-GAPS`/AEC/NID/SNID/OTZ but no `SID`/`HSID`/`SSID`. No material change vs 23:59 CDT; cutover remains blocked on Homey-owned DNS. **Not complete:** oldfashioneddoctor.com still serves Blogger, not Astro; no login was attempted.

Keeper `2026-09-19 23:59 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200; real post has Web3Forms markers). Apex still redirects to `www`, and both remain Blogger/GSE over HTTPS. DNS via public DoH/getent (box lacks `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. `cusdis.com` remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; latest `site` workflow is green. No newly available Squarespace/Google DNS login (Squarespace session/analytics + Google AEC/NID/GAPS only). No material change vs 23:57 CDT; Homey DNS action remains the blocker.
Keeper `2026-09-19 23:57 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a` brand colors, post covers, Drinks nav); `/drinks/` HTTP 200 and a real post route contains Web3Forms markers. Apex still 301-redirects to `www`; both continue serving Blogger/GSE over HTTPS. Public DNS still returns apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c1/c2/c3/c4.googledomains.com` (DoH/getent; box lacks `dig`/`nslookup`). `cusdis.com` remains HTTP **521**; comments remain Web3Forms. GitHub Pages API remains `status: built`, `cname: null`, `https_enforced: true`; latest `site` workflow is green. Light browser cookie check: Squarespace has `SS_SESSION_ID`/analytics/`crumb`/`ANONYMOUS_ID` only (no Domains-capable login); Google has `__Host-GAPS`/OTZ but no `SID`/`HSID`/`SSID`. No material change vs 20:52 CDT; cutover remains blocked on Homey-owned DNS. **Not complete:** oldfashioneddoctor.com still serves Blogger, not Astro; no login was attempted.

Keeper `2026-09-19 20:52 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a` brand colors, post covers, Drinks nav); `/drinks/` HTTP 200 and a real post route contains Web3Forms markers. Apex still 301-redirects to `www`; both continue serving Blogger/GSE over HTTPS. Public DNS still returns apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c1/c2/c3/c4.googledomains.com` (DoH/getent; box lacks `dig`/`nslookup`). `cusdis.com` remains HTTP **521**; comments remain Web3Forms. GitHub Pages API remains `status: built`, `cname: null`, `https_enforced: true`; latest `site` workflow is green. Light browser cookie check: Squarespace has `SS_SESSION_ID`/analytics/`crumb`/`ANONYMOUS_ID` only (no Domains-capable login); Google has AEC/NID/SNID/`__Host-GAPS` but no `SID`/`HSID`/`SSID`. No material change vs 19:51 CDT; cutover remains blocked on Homey-owned DNS. **Not complete:** oldfashioneddoctor.com still serves Blogger, not Astro; no login was attempted.

Keeper `2026-09-19 19:51 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a` brand colors, post covers, Drinks nav); `/drinks/` HTTP 200 and a real post route contains Web3Forms markers. Apex still 301-redirects to `www`; both continue serving Blogger/GSE over HTTPS. Public DNS still returns apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c1/c2/c3/c4.googledomains.com` (DoH/getent; box lacks `dig`/`nslookup`). `cusdis.com` remains HTTP **521**; comments remain Web3Forms. GitHub Pages API remains `status: built`, `cname: null`, `https_enforced: true`; latest `site` workflow is green. Light browser cookie check: Squarespace has `SS_SESSION_ID`/analytics/`crumb`/`ANONYMOUS_ID` only (no Domains-capable login); Google has AEC/NID/SNID/`__Host-GAPS` but no `SID`/`HSID`/`SSID`. No material change vs 18:53 CDT; cutover remains blocked on Homey-owned DNS. **Not complete:** oldfashioneddoctor.com still serves Blogger, not Astro; no login was attempted.

Keeper `2026-09-19 18:53 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a` brand colors, post covers, Drinks nav); `/drinks/` HTTP 200 and a real post route contains Web3Forms markers. Apex still 301-redirects to `www`; both continue serving Blogger/GSE over HTTPS. Public DNS still returns apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c1/c2/c3/c4.googledomains.com` (DoH/getent; box lacks `dig`/`nslookup`). `cusdis.com` remains HTTP **521**; comments remain Web3Forms. GitHub Pages API remains `status: built`, `cname: null`, `https_enforced: true`; latest `site` workflow is green. Light browser cookie check: Squarespace has `SS_SESSION_ID`/analytics/`crumb`/`ANONYMOUS_ID` only (no Domains-capable login); Google has AEC/NID/SNID/`__Host-GAPS` but no `SID`/`HSID`/`SSID`. No material change vs 17:59 CDT; cutover remains blocked on Homey-owned DNS. **Not complete:** oldfashioneddoctor.com still serves Blogger, not Astro; no login was attempted.

Keeper `2026-09-19 17:59 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200; real post has Web3Forms markers). Apex still redirects to `www`, and both remain Blogger/GSE over HTTPS. DNS via public DoH/getent (box lacks `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. `cusdis.com` remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; latest `site` workflow is green. No newly available Squarespace/Google DNS login (Squarespace session/analytics + Google AEC/NID/GAPS only). No material change vs 16:51 CDT; Homey DNS action remains the blocker.

Keeper `2026-09-19 16:51 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200; real post has Web3Forms markers). Apex still redirects to `www`, and both remain Blogger/GSE over HTTPS. DNS via public DoH/getent (box lacks `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. `cusdis.com` remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; latest `site` workflow is green. No newly available Squarespace/Google DNS login (Squarespace session/analytics + Google AEC/NID/GAPS only). No material change vs 15:49 CDT; Homey DNS action remains the blocker.

Keeper `2026-09-19 15:49 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200; real post has Web3Forms markers). Apex still redirects to `www`, and both remain Blogger/GSE over HTTPS. DNS via public DoH/getent (box lacks `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. `cusdis.com` remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; latest `site` workflow is green. No newly available Squarespace/Google DNS login (anonymous Squarespace + Google GAPS/AEC/NID only). No material change vs 14:56 CDT; Homey DNS action remains the blocker.

Keeper `2026-09-19 14:56 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200; real post has Web3Forms markers). Apex still redirects to `www`, and both remain Blogger/GSE over HTTPS. DNS via public DoH/getent (box lacks `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. `cusdis.com` remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; latest `site` workflow is green. No newly available Squarespace/Google DNS login was found in a light browser check. No material change vs 13:49 CDT; Homey DNS action remains the blocker.
Keeper `2026-09-19 13:49 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200); a real post route is 200 with Web3Forms `access_key` comments. Apex still redirects to `www`, and both remain Blogger/GSE. DNS via DoH/getent (no `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. Cusdis remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; no usable Google/Squarespace DNS session (Squarespace anonymous/session cookies only). No material change vs 12:54 CDT; Homey DNS action remains the blocker.

Keeper `2026-09-19 12:54 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200); a real post route is 200 with Web3Forms `access_key` comments. Apex still redirects to `www`, and both remain Blogger/GSE. DNS via DoH/getent (no `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. Cusdis remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; no usable Google/Squarespace DNS session this run. No material change vs 11:56 CDT; Homey DNS action remains the blocker.

Keeper `2026-09-19 11:56 CDT`: preview remains healthy (curl HTTP 200 Astro; cream/amber theme, covers, Drinks nav; `/drinks/` 200); a real post route is 200 with Web3Forms `access_key` comments. Apex still redirects to `www`, and both remain Blogger/GSE. DNS via DoH/getent (no `dig`/`nslookup`) remains Google/Blogger A `216.239.32.21/.34.21/.36.21/.38.21`, `www` CNAME `ghs.google.com`, and `ns-cloud-c*.googledomains.com`. Cusdis remains **521**; Pages API remains built, `cname: null`, HTTPS enforced; no usable Google/Squarespace DNS session (only Squarespace anonymous/session cookies, no Google SID/HSID/SSID). No material change vs 10:56 CDT; Homey DNS action remains the blocker.

# Old Fashioned Doctor — go-live status

Keeper `2026-09-19 10:56 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber theme, post covers, Drinks nav); `/drinks/` HTTP 200 and live CSS contains cream/amber colors. Apex `https://oldfashioneddoctor.com/` still 301-redirects to `www`, and both serve Blogger/GSE over HTTPS (Blogger markers). Live DNS remains Homey-owned: apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; `www` CNAME `ghs.google.com` (Google frontend A); NS `ns-cloud-c1/c2/c3/c4.googledomains.com`. `dig`/`nslookup` are unavailable on the box; records were independently checked via resolver/DoH and `getent`. Hosted `cusdis.com` remains HTTP **521**; comments stay on Web3Forms and were not changed. GitHub Pages API reports `status: built`, `cname: null`, `https_enforced: true` (API source still `main`); preview remains healthy. No usable Squarespace/Google auth session: 15 scoped headless Chrome cookie DBs contain none of the checked auth names; the regular `chrome-profile` has Squarespace `crumb`/`ANONYMOUS_ID`/`SS_SESSION_ID` and Google `__Host-GAPS`/AEC/NID/SNID, but no Google `SID`/`HSID`/`SSID` and no Domains-capable login. No material change vs 09:54 CDT; cutover remains blocked on Homey-owned DNS and requires Homey's DNS action. **Not complete:** oldfashioneddoctor.com still serves Blogger, not Astro; no Homey login was attempted.


Keeper `2026-09-19 09:54 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace crumb/`ANONYMOUS_ID`/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 08:53 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-19 08:53 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace crumb/`ANONYMOUS_ID`/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 07:51 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-19 07:51 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace crumb/`ANONYMOUS_ID`/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 06:54 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-19 06:54 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace crumb/`ANONYMOUS_ID`/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 05:50 CDT; no cutover and no WakeParent (before 7:30 CDT).

Keeper `2026-09-19 05:50 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace crumb/`ANONYMOUS_ID`/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 04:58 CDT; no cutover and no WakeParent (before 7:30 CDT).

Keeper `2026-09-19 04:58 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace crumb/`ANONYMOUS_ID`/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 03:58 CDT; no cutover and no WakeParent (before 7:30 CDT).

Keeper `2026-09-19 02:57 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace crumb/`ANONYMOUS_ID`/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 01:59 CDT; no cutover and no WakeParent (before 7:30 CDT).

Keeper `2026-09-19 01:59 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace crumb/`ANONYMOUS_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 00:58 CDT; no cutover and no WakeParent (before 7:30 CDT).

Keeper `2026-09-19 00:58 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace crumb/`ANONYMOUS_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 23:50 CDT; no cutover and no WakeParent (before 7:30 CDT).

Keeper `2026-09-18 23:50 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 22:51 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 22:51 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 21:57 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 21:57 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 20:59 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 20:59 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 19:59 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 19:59 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 18:55 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 18:55 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 17:54 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 17:54 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 16:04 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 16:04 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 15:03 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 15:03 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 14:01 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 14:01 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 13:02 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 13:02 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 12:13 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 12:13 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 11:15 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 11:15 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 10:11 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 10:11 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records; NS `ns-cloud-c*.googledomains.com`). No material change vs 09:17 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 09:17 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 08:04 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 08:04 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 06:54 CDT; no cutover and no WakeParent (after 7:30 CDT).

Keeper `2026-09-18 06:54 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 05:56 CDT; no cutover and no WakeParent (before 7:30 CDT).

Keeper `2026-09-18 05:56 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID; chrome-profile has Squarespace analytics/`SS_SESSION_ID` but not a Domains-capable login). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 04:58 CDT; no cutover and no WakeParent (before 7:30 CDT).

Keeper `2026-09-18 04:58 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 03:59 CDT; no cutover and no WakeParent.

Keeper `2026-09-18 03:59 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 02:57 CDT; no cutover and no WakeParent.

Keeper `2026-09-18 02:57 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; no SID/HSID/SSID session). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 01:59 CDT; no cutover and no WakeParent.

Keeper `2026-09-18 01:59 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 00:49 CDT; no cutover and no WakeParent.

Keeper `2026-09-18 00:49 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; headless Chrome cookie DBs empty of SID/HSID/SSID). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 23:55 CDT; no cutover and no WakeParent.

Keeper `2026-09-17 23:55 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / ghs). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (apex still Blogger Google A records). No material change vs 22:57 CDT; no cutover and no WakeParent.

Keeper `2026-09-17 22:57 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www CNAME `ghs.google.com` and Google frontend A). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 21:55 CDT; no cutover and no WakeParent.

Keeper `2026-09-17 21:55 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www CNAME `ghs.google.com` and Google frontend A). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 20:58 CDT; no cutover and no WakeParent.

Keeper `2026-09-17 20:58 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www CNAME `ghs.google.com` and Google frontend A). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 19:57 CDT; no cutover and no WakeParent.

Keeper `2026-09-17 19:57 CDT`: preview freshly verified via curl HTTP 200 as Astro (title/brand, cream/amber `#f4ead8`/`#d9c48a`, covers, Drinks nav); `/drinks/` HTTP 200. A post page HTTP 200 contains the Web3Forms endpoint/access-key form markers; comments remain Web3Forms, not Cusdis. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Blogger markers; apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www CNAME `ghs.google.com` and Google frontend A). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. Browser tabs show Squarespace/Google login pages rather than an authenticated DNS session; no usable login is available. DNS remains Homey-owned (NS `ns-cloud-c1/c2/c3/c4.googledomains.com`). No material change vs 19:02 CDT; no cutover and no WakeParent.

Overnight / morning: site work is **done** on branch `site`. Essay prose untouched. All **24** posts remain. Blogger comments **not** imported.

Keeper `2026-09-17 19:02 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Powered by Blogger; apex DNS `216.239.32/34/36/38.21`; www CNAME `ghs.google.com`). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 18:00 CDT; no WakeParent (after 7:30 CDT).

Keeper `2026-09-17 18:00 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Powered by Blogger; apex DNS `216.239.32/34/36/38.21`; www CNAME `ghs.google.com`). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (login.squarespace.com still `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 16:55 CDT; no WakeParent (after 7:30 CDT).

Keeper `2026-09-17 16:55 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Powered by Blogger; apex DNS `216.239.32/34/36/38.21`; www CNAME `ghs.google.com`). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (no non-scoped Chrome cookie DB; headless scopes empty for Squarespace/Google auth cookies). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 15:58 CDT; no WakeParent (after 7:30 CDT).

Keeper `2026-09-17 15:58 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Powered by Blogger; apex DNS `216.239.32/34/36/38.21`; www CNAME `ghs.google.com`). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (ANONYMOUS_ID on login.squarespace.com; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 14:59 CDT; no WakeParent (after 7:30 CDT).

Keeper `2026-09-17 14:59 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Powered by Blogger; apex DNS `216.239.32/34/36/38.21`; www CNAME `ghs.google.com`). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (ANONYMOUS_ID on login.squarespace.com; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 14:05 CDT; no WakeParent (after 7:30 CDT).


Keeper `2026-09-17 14:05 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (Powered by Blogger; apex DNS `216.239.32/34/36/38.21`; www CNAME `ghs.google.com`). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (ANONYMOUS_ID on login.squarespace.com; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 13:05 CDT; no WakeParent (after 7:30 CDT).



`npm run build` **green** (Astro + Pagefind indexed 24 pages).

## Live preview (use this until DNS cutover)

**https://dkrics.github.io/oldfashioneddoctor/**

Hard-refresh. Cream/amber Astro site with `Drinks` nav, per-post covers, and enabled comments form — **not** Blogger.

Repo is intentionally on **github.io preview mode** until you flip Squarespace DNS:
- `astro.config.mjs`: `site: 'https://dkrics.github.io'`, `base: '/oldfashioneddoctor/'`
- No `public/CNAME` (would 301 github.io → Blogger while apex is still `216.239.*`)
- Restore files: `CNAME.example.live` / `CNAME.example` → copy to `public/CNAME` after DNS

## What landed

- Real `hero` on every post (12 generated covers in `public/images/covers/` + `public/images/og-default.jpg`) — no gradient placeholders
- SEO: titles, descriptions, canonicals, OG/Twitter, sitemap, robots, RSS, JSON-LD `Blog` / `BlogPosting`, author **Old Fashioned Doctor** only
- Drinks rename (`/cocktails/` → `/drinks/`), Pagefind search, Blogger path redirects under `public/2017`, `public/2018`, `public/p`
- Feedback via X only: [@OldFashionedDr](https://x.com/OldFashionedDr)
- Comments UI (`Comments.astro` + Web3Forms email POST): Name / Email / Comment required. Email never published. No on-page thread — notes go to the private inbox for review. **Enabled** with `PUBLIC_WEB3FORMS_ACCESS_KEY`. X remains the public alternate.

## Git

- Branch `site` is the deploy branch (Actions workflow on push to `site`)
- `gh` authenticated as **DKrics** (`repo` + `workflow`)
- Deploy stays on **`site`** (Actions). Do **not** force-push `main`
- `origin/main` is a divergent simpler rebuild; agents will not force-push

## Blocked on Homey (no live DNS changes from agents)

1. **Squarespace DNS** → GitHub Pages (apex still Blogger `216.239.32/34/36/38.21`)
2. Box browser login for Squarespace/Google (anonymous cookies only; still at sign-in walls)

Exact click-paths: see `MORNING-STATUS.md`.

## Cover files

```
public/images/covers/near-death-experience.jpg
public/images/covers/whiskey-healthcare.jpg
public/images/covers/martini-monday.jpg
public/images/covers/barrel-aging.jpg
public/images/covers/health-weight-loss.jpg
public/images/covers/heres-to-21.jpg
public/images/covers/cocktail-fail.jpg
public/images/covers/misinformed-patients.jpg
public/images/covers/waiting-on-doctor.jpg
public/images/covers/rosh-hashana.jpg
public/images/covers/good-doctors.jpg
public/images/covers/medicare-plan.jpg
public/images/og-default.jpg
```

Keeper `2026-09-15 20:00 CDT`: preview re-verified via curl HTTP 200 as Astro (cream/amber, covers, Drinks); `/drinks/` HTTP 200. Apex and `www` still Blogger; Cusdis HTTP 521; GitHub Pages `cname` null with HTTPS enforced; no usable Squarespace/Google session on box (anonymous-only). No material change vs 18:54 CDT; DNS still Homey-owned; no WakeParent.

Keeper `2026-09-15 21:52 CDT`: preview re-verified via curl HTTP 200 as Astro (cream/amber, covers, Drinks); `/drinks/` HTTP 200. Apex and `www` still Blogger; Cusdis HTTP 521; GitHub Pages `cname` null with HTTPS enforced; no usable Squarespace/Google session on box (anonymous-only). No material change vs 20:53 CDT; DNS still Homey-owned; no WakeParent.

Keeper `2026-09-15 22:50 CDT`: preview re-verified via curl HTTP 200 as Astro (cream/amber, covers, Drinks); `/drinks/` HTTP 200. Apex and `www` still Blogger; Cusdis HTTP 521; GitHub Pages `cname` null with HTTPS enforced; no usable Squarespace/Google session on box (anonymous-only). No material change vs 21:52 CDT; DNS still Homey-owned; no WakeParent.

- Keeper `2026-09-15 23:49 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor brand, cream/amber markers, covers, and Drinks); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both apex and `www` serve Blogger/GSE over HTTPS (not Astro). Hosted Cusdis remains HTTP 521. GitHub Pages API reports `cname: null`, HTTPS enforced, workflow build; `gh` is authenticated. Light browser-cookie check found no Squarespace/Google auth cookies; no usable DNS session is available. DNS remains Homey-owned. No material change; no WakeParent (after 7:30 CDT).

Keeper `2026-09-16 00:57 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor brand, cream/amber markers, covers, and Drinks); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both apex and `www` serve Blogger/GSE over HTTPS (not Astro). Hosted Cusdis remains HTTP 521. GitHub Pages API reports `cname: null`, HTTPS enforced, workflow build; `gh` is authenticated. Light browser-cookie check: login.squarespace.com still has `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID. DNS remains Homey-owned. No material change; no WakeParent (before 7:30 CDT).

Keeper `2026-09-16 02:00 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor brand, cream/amber markers, covers, and Drinks); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both apex and `www` serve Blogger/GSE over HTTPS (not Astro). Hosted Cusdis remains HTTP 521. GitHub Pages API reports `cname: null`, HTTPS enforced, workflow build; `gh` is authenticated. Light browser-cookie check: login.squarespace.com still has `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID. DNS remains Homey-owned. No material change; no WakeParent (before 7:30 CDT).

Keeper `2026-09-16 03:02 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor brand, cream/amber markers, covers, and Drinks); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both apex and `www` serve Blogger/GSE over HTTPS (not Astro). Hosted Cusdis remains HTTP 521. GitHub Pages API reports `cname: null`, HTTPS enforced, workflow build; `gh` is authenticated. Light browser-cookie check: login.squarespace.com still has `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID. DNS remains Homey-owned. No material change; no WakeParent (before 7:30 CDT).

Keeper `2026-09-16 06:55 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber brand colors `#f4ead8`/`#d9c48a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (generator=blogger; apex DNS `216.239.32/34/36/38.21`; www Google frontends). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (no Squarespace cookies in available Chrome DBs; no Google SID/HSID/SSID). DNS remains Homey-owned. No material change vs 05:59 CDT; no WakeParent (before 7:30 CDT).

Keeper `2026-09-16 08:04 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (apex DNS `216.239.32/34/36/38.21`; www Google frontends). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (login.squarespace.com `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned. No material change vs 06:55 CDT; no WakeParent (after 7:30 CDT; DNS ask unchanged).

Keeper `2026-09-16 20:00 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (apex DNS `216.239.32/34/36/38.21`; www CNAME `ghs.google.com`). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (login.squarespace.com `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 09:51 CDT; no WakeParent (after 7:30 CDT; DNS ask unchanged).

Keeper `2026-09-16 21:51 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (apex DNS `216.239.32/34/36/38.21`; www CNAME `ghs.google.com`). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (login.squarespace.com `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 20:58 CDT; no WakeParent (after 7:30 CDT; DNS ask unchanged).

- Keeper `2026-09-16 22:57 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (apex DNS `216.239.32/34/36/38.21`; www → Google frontends / ghs). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (login.squarespace.com `ANONYMOUS_ID` + account redirects to Login; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (prior NS googledomains). No material change vs 21:51 CDT; no WakeParent (after 7:30 CDT; DNS ask unchanged).

Keeper `2026-09-17 01:54 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (apex DNS `216.239.32/34/36/38.21`; www → Google frontends / ghs / CNAME `ghs.google.com`). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (login.squarespace.com `ANONYMOUS_ID`; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS googledomains). No material change vs 00:53 CDT; no WakeParent (before 7:30 CDT).

- Keeper `2026-09-17 06:50 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (generator=blogger; apex DNS `216.239.32/34/36/38.21`; www CNAME `ghs.google.com`). Hosted Cusdis remains HTTP **521** (abandoned); comments Web3Forms form remains enabled (`access_key` present on post pages). GitHub Pages API still reports `cname: null`, `https_enforced: true`, workflow built; latest `site` deploy green. No usable Squarespace/Google session on the box (account/domains hit login walls; Google cookies lack SID/HSID/SSID). DNS remains Homey-owned (NS `ns-cloud-c*.googledomains.com`). No material change vs 05:51 CDT; no WakeParent (before 7:30 CDT).

Keeper `2026-09-19 03:58 CDT`: preview re-verified via curl HTTP 200 as Astro (Old Fashioned Doctor title/brand, cream/amber `#f4ead8`/`#d9c48a`/`#6b3e1a`, covers, Drinks nav); `/drinks/` HTTP 200 and a valid post still contains the Web3Forms comments form. Apex redirects 301 to `www`, then both serve Blogger/GSE over HTTPS (apex A `216.239.32.21`, `.34.21`, `.36.21`, `.38.21`; www → Google frontend / CNAME `ghs.google.com`). Hosted `cusdis.com` remains HTTP **521**. GitHub Pages API still reports `cname: null`, `https_enforced: true`; latest `site` deploy is green. Available box Chrome profiles show no usable Squarespace/Google auth session. DNS remains Homey-owned. No material change vs 02:57 CDT; no cutover and no WakeParent (before 7:30 CDT).
