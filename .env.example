# Public at build time (appears in the browser bundle as the Web3Forms access key).
# GitHub Actions: Settings → Secrets and variables → Actions → PUBLIC_WEB3FORMS_ACCESS_KEY
# Create the key at https://web3forms.com (private inbox email; never published on the site).
PUBLIC_WEB3FORMS_ACCESS_KEY=
